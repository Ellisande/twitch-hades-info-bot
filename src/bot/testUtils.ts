const testUserId = "ellisandepoet";
const testChannelId = "#ellisandepoet";
const isTestChannel = (channel: string) => channel == testChannelId;
const isTestUser = (user: string) => user == testUserId;

export { isTestChannel, isTestUser, testUserId, testChannelId };
